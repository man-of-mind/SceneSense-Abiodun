# Plot usage note

Use `presentation/` for current reporting and slides.

The presentation plots use the optimized pipeline as the default and exclude the retired legacy-rasterizer
comparison. Root-level plots are exploratory/provenance artifacts from the analysis scripts and should not be
used directly in slides without checking that they match the current reporting story.
