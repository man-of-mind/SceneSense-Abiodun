{
  "name": "disc_lr_v1",
  "_arm": "Option A, discriminative learning rate: inherited RGB/LR-ASPP backbone, segmentation classifier and coarse object head FROZEN; only the radar encoder, hybrid FPN, refinement trunk and the three refinement output heads train, at 3e-4",
  "_warm_start": "hybrid_centerfusion_v1/20260826_162833 warm_start.pt sha256=935d7c10afe10580afbda7b5691a9985358b846c421199fb99fd761abacd424b (parity-verified; loaded strict, read-only, never overwritten)",
  "_unchanged_from_failed_run": "architecture, loss, targets, evaluator and decoder are byte-identical; optimizer type, weight decay, batch, workers, augmentation, schedule shape and training seed (data order) are identical; only lr and the three freeze flags differ",
  "optimizer": "adamw",
  "lr": 0.0003,
  "_lr_note": "10x the failed run's 3e-5. Single value, no sweep.",
  "weight_decay": 0.0001,
  "augment_strength": "strong",
  "geometric_augment": false,
  "input_size": [768, 432],
  "batch_size": 16,
  "num_workers": 8,
  "prefetch_factor": 2,
  "persistent_workers": true,
  "epochs": 24,
  "run_until_epoch": 6,
  "checkpoint_epoch_offset": 1,
  "_schedule_note": "epochs=24 with run_until_epoch=6 keeps the cosine SHAPE identical to the failed run (lr rides 3.00e-4 -> 2.67e-4 across the six epochs, exactly 10x the previous 3.00e-5 -> 2.67e-5), so the only changed variable is the LR magnitude and the freeze set. Six epochs total; the run stops at 6.",
  "early_stop_patience": 24,
  "init_rgb_checkpoint": "",
  "init_object_checkpoint": "",
  "_init_note": "empty on purpose: the whole model state is loaded strict from warm_start.pt by the entry point, so all 379 tensors are bit-identical to the parity-verified warm start",
  "freeze_backbone": true,
  "freeze_classifier": true,
  "freeze_object_head": true,
  "freeze_bn": true,
  "lr_scheduler": "cosine",
  "lr_warmup_epochs": 1,
  "min_lr_ratio": 0.01,
  "poly_power": 0.9,
  "selection_score_mode": "loc_dim_loss",
  "class_loss_weights": [0.5, 1.0, 4.0],
  "lovasz_weight": 0.5,
  "loss_weights": {
    "object_total": 1.0,
    "segmentation": 0.3,
    "object": {
      "center": 4.0,
      "location": 1.5,
      "dimensions": 0.6,
      "yaw": 0.3,
      "parked": 0.2,
      "radar_support": 0.1,
      "bbox2d": 1.0
    }
  },
  "object_heads": {
    "heatmap_radius_px": 4,
    "fuse_low_feature": true,
    "head_arch": "centerfusion_hybrid_v1",
    "use_coordconv": false,
    "head_depth": 3,
    "use_groundplane_prior": false,
    "predict_bbox2d": true,
    "adaptive_heatmap_radius": true,
    "vehicle_heatmap_radius_cap_px": 4,
    "max_gt_distance_m": 40
  },
  "feature_drop_max": 0.0,
  "feature_drop_val": 0.0,
  "ae_bottleneck": 0,
  "training_seed": 20260826,
  "checkpoint_every_epochs": 1
}
