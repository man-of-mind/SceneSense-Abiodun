"""Fail-closed numerical recovery implementation for SplitFusion FCOS V1.

Importing this package never loads a checkpoint, dataset, model, or CUDA runtime.
The package remains unusable for scientific continuation until its qualification
artifacts bind a reviewed source state to a selected yaw floor and measured
pre-step ceilings.
"""

RECOVERY_STATE = "UNQUALIFIED_IMPLEMENTATION_ONLY"
